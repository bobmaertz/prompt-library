---
name: spec
description: Translate a feature request or issue into a structured spec with acceptance criteria, API contracts, and error cases. Output is ready for /plan. Use before /plan on any non-trivial feature to ensure requirements are clear before architecture begins.
argument-hint: <feature request or issue description>
allowed-tools: Read, Glob, Grep, Write
---

Invoke the `pm-spec-writer` skill to translate the following request into a structured spec:

$ARGUMENTS
